title:38 Counts occurrences of characters in a Given String - JavaScript
tags: JavaScript

```JavaScript
let str = "Hello World!";
let charFrequency = {};

for (let i = 0; i < str.length; i++) {
  let char = str.charAt(i);
  if (charFrequency[char]) {
    charFrequency[char]++;
  } else {
    charFrequency[char] = 1;
  }
}

console.log(charFrequency);
```